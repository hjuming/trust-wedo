{
  "name": "trust-wedo-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit -p apps/api-worker/tsconfig.json",
    "worker:dev": "wrangler dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.91.0"
  }
}
