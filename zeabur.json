{
    "serviceName": "trust-wedo-backend",
    "buildCommand": "pip install -r apps/backend/requirements.txt && pip install .",
    "startCommand": "export PYTHONPATH=$PYTHONPATH:apps/backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "outputDirectory": "."
}