{
    "serviceName": "trust-wedo-backend",
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "export PYTHONPATH=$PYTHONPATH:. && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "outputDirectory": "."
}